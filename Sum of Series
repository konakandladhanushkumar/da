#include <stdio.h>
#include <math.h>

int main() {
    int x, n;
    double sum = 0;

    scanf("%d%d", &x, &n);

    for(int i = 0; i <= n; i++)
        sum = sum + pow(x, i);

    printf("Sum = %.0lf\n", sum);
    return 0;
}
